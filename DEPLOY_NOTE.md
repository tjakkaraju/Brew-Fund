# Deploy probe

This is a tiny liveness write to confirm the GitHub MCP bridge is responsive before pushing the full 109KB index.html. If this file appears in the repo, the bridge is alive and the dashboard push will follow.
